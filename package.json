{
  "name": "undup",
  "version": "1.0.0",
  "description": "Remove duplicate chrome tabs",
  "main": "index.js",
  "scripts": {
    "build": "tsc",
    "watch": "tsc -w",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "devDependencies": {
    "typescript": "^3.0.3"
  }
}
